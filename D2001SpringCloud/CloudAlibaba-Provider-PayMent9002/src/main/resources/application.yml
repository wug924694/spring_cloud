server:
  port: 9002

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848