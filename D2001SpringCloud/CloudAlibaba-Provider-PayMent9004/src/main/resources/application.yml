server:
  port: 9004

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   #nacos地址