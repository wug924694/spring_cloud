server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:  #将当前服务注册进nacos
        server-addr: localhost:8848
      config:     #配置中心
        server-addr: localhost:8848
        file-extension: yaml  #指定配置文件类型
        #group: DEFAULT_GROUP # 指定组名称
